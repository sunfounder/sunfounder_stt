# sunfounder_stt
requests>=2.25

# Optional backends (uncomment as needed):
# faster-whisper>=1.0   # local_fast backend (CTranslate2, int8 CPU)
# pywhispercpp>=0.1     # local_standard backend (whisper.cpp GGML)
